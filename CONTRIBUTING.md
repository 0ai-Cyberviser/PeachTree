# Contributing

Required checks:

```bash
pytest -q
ruff check src tests
```

Required principles:

- provenance-first records
- local-only defaults
- public GitHub opt-in only
- license-aware collection
- secret filtering
- no unnecessary dependencies
